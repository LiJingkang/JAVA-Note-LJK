* JUnit 测试不成功
    * libray 是否将 JUnit 添加了进去。
    * JUnit 默认使用的是 系统环境变量里面的 JRE。 检查是否和运行时使用的一致。

* 成功进入debug界面之后，从右上角找到breakpoint选项（三个选项中的中间一个）。

    会看到一排图标，一个X号的表示取消选择的debug断点，两个X号的是取消所有断点。选择两个X号的，点击。

    弹出来的对话框，点击yes，就可以取消所有的断点了。